# Legacy Assets

`legacy/docs/` is archival reference only and is not part of active app runtime/deploy.

Canonical jobs data path for backend scripts/workflows is `data/master_jobs.json`.

The deploy artifact for the web app is `web/dist` only.
